# FPL source fixtures

These small JSON documents represent the upstream endpoint shapes used by the source-adapter tests. When the upstream schema changes, refresh them from a sanitized response and update the adapter test before changing normalization rules. Do not commit private league or account data.

